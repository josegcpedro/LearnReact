export default function Home() {
  // ici créez un useState qui va garder la valeur des click, et un use effect qui va être appelé uniquement quand on click sur "reset" si le compteur est plus haut que 0
  return (
    // affichez après main 2 bouton (1 pour le compteur, 1 pour le reset) et un h1 qui contient la valeur des click.
    <main>
    </main>
  );
}

// verifie ta réponse à la fin en fesant un "git checkout done-project"
// pour revenir ici après avoir vérifier la réponse fait un "git checkout main"
